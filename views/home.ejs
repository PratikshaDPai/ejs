<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Rendering an EJS page</h1>
    <% let showMessage = true; %> <% if(showMessage===true) { %>
    <p>This is a dynamic message!</p>
    <% } else { %>
    <p>Something else!</p>
    <% } %>
    <!-- output tags -->
    <p>2 plus 2 is <%= 2 + 2 %></p>
  </body>
</html>
